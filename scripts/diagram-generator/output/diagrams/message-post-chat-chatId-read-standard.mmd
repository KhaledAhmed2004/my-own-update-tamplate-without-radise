sequenceDiagram
    autonumber
    participant Client
    participant Route
    participant Auth_Middleware
    participant Controller
    participant Service
    participant Database
    participant Helper_setUnreadCount

    Client->>+Route: POST /chat/:chatId/read
    Route->>+Auth_Middleware: Validate
    Note over Auth_Middleware: 🔐 Authentication&#58; JWT token verify করা হচ্ছে
    Note over Auth_Middleware: Roles&#58; USER_ROLES.TASKER, USER_ROLES.POSTER
    Auth_Middleware->>-Route: Valid ✓
    Route->>+Controller: MessageController.markChatRead
    Controller->>+Service: MessageService.markChatAsRead()
    Service->>+Database: Message.find({
    chatId,
    sender: { $ne: userId },
    )
    Note over Database: 💾 Database Query&#58; MongoDB থেকে data fetch করা হচ্ছে
    Database->>-Service: result
    Service->>+Database: Message.updateMany({ _id: { $in: toUpdate.map(m => m._id)
    Note over Database: 💾 Database Query&#58; MongoDB থেকে data fetch করা হচ্ছে
    Database->>-Service: result
    Service->>Helper_setUnreadCount: setUnreadCount()
    Helper_setUnreadCount-->>Service: result
    Service->>-Controller: result
    Controller->>-Route: StatusCodes.OK - Chat messages marked as read
    Route-->>Client: Response
    Note over Client: ✅ Chat messages marked as read
