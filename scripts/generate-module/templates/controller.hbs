import { Request, Response } from 'express';
import { StatusCodes } from 'http-status-codes';
import catchAsync from '../../../shared/catchAsync';
import sendResponse from '../../../shared/sendResponse';
import { {{pascalCase}}Service } from './{{moduleName}}.service';

/**
 * Create {{displayName}}
 */
const create{{pascalCase}} = catchAsync(async (req: Request, res: Response) => {
  const result = await {{pascalCase}}Service.create{{pascalCase}}ToDB(req.body);

  sendResponse(res, {
    statusCode: StatusCodes.CREATED,
    success: true,
    message: '{{displayName}} created successfully',
    data: result,
  });
});

/**
 * Get {{displayName}} by ID
 */
const get{{pascalCase}}ById = catchAsync(async (req: Request, res: Response) => {
  const { id } = req.params;
  const result = await {{pascalCase}}Service.get{{pascalCase}}ByIdFromDB(id);

  sendResponse(res, {
    statusCode: StatusCodes.OK,
    success: true,
    message: '{{displayName}} retrieved successfully',
    data: result,
  });
});

/**
 * Get all {{displayName}}s
 */
const getAll{{pascalCase}}s = catchAsync(async (req: Request, res: Response) => {
  const result = await {{pascalCase}}Service.getAll{{pascalCase}}sFromDB(req.query);

  sendResponse(res, {
    statusCode: StatusCodes.OK,
    success: true,
    message: '{{displayName}}s retrieved successfully',
{{#if hasPagination}}
    pagination: result.pagination,
{{/if}}
    data: result.data,
  });
});

/**
 * Update {{displayName}}
 */
const update{{pascalCase}} = catchAsync(async (req: Request, res: Response) => {
  const { id } = req.params;
  const result = await {{pascalCase}}Service.update{{pascalCase}}ToDB(id, req.body);

  sendResponse(res, {
    statusCode: StatusCodes.OK,
    success: true,
    message: '{{displayName}} updated successfully',
    data: result,
  });
});

/**
 * Delete {{displayName}}
 */
const delete{{pascalCase}} = catchAsync(async (req: Request, res: Response) => {
  const { id } = req.params;
  const result = await {{pascalCase}}Service.delete{{pascalCase}}FromDB(id);

  sendResponse(res, {
    statusCode: StatusCodes.OK,
    success: true,
    message: '{{displayName}} deleted successfully',
    data: result,
  });
});

export const {{pascalCase}}Controller = {
  create{{pascalCase}},
  get{{pascalCase}}ById,
  getAll{{pascalCase}}s,
  update{{pascalCase}},
  delete{{pascalCase}},
};
